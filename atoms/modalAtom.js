import { atom } from 'recoil'

export const modalState = atom({
  key: 'modalState',
  default: false,
})
export const modalStateComment = atom({
  key: 'modalStateComment',
  default: false,
})

